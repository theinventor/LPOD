<%= form_for @landing_page, :html => {:multipart => true} do |f| %>
  <% if @landing_page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@landing_page.errors.count, "error") %> prohibited this landing_page from being saved:</h2>

      <ul>
      <% @landing_page.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :url %>
    <%= f.text_field :url %>
  </div>
  <div class="field">
    <%= f.collection_select :industry_id, Industry.find(:all), :id, :industry, :prompt => "Select a Industry" %>
  </div>
  <div class="field">
    <%= f.collection_select :landing_page_type_id, LandingPageType.find(:all), :id, :types, :prompt => "Select a Landing Page Type" %>
  </div>

  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
